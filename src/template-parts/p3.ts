export default "const signed=Boolean(me.user);$('authView').hidden=signed;$('dashboard').hidden=!signed;if(signed){$('welcome').textContent='Welcome, '+me.user.name;$('accountEmail').textContent=me.user.email;await loadFiles()}}async function loadFiles(){const data=await api('/api/files');$('files').replaceChildren(...data.files.map(file=>{const row=document.createElement('div');row.className='file';const info=document.createElement('span');info.textContent=file.name+' · '+Math.ceil(file.size/1024)+' KB';const link=document.createElement('a');link.href='/files/'+encodeURIComponent(file.id);link.textContent='Open';link.target='_blank';row.append(info,link);return row}))}for(const id of ['signupForm','loginForm']){$(id).addEventListener('submit',async event=>{event.preventDefault();const form=new FormData(event.currentTarget);try{say('Working…');await api('/api/'+(id==='signupForm'?'signup':'login'),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(form))});event.currentTarget.reset();say('Success');await refresh()}catch(error){say(error.message,true)}})}$('logout').onclick=async()=>{await api('/api/logout',{method:'POST'});say('Signed out');await refresh()};$('uploadForm').addEventListener('submit',async event=>{event.preventDefault();try{say('Uploading…');await api('/api/files',{method:'POST',body:new FormData(event.currentTarget)});event.currentTarget.reset();say('Uploaded');await loadFiles()}catch(error){say(error.message,true)}});refresh().catch(error=>say(error.message,true));`;\n\nfunction json(value, status = 200, headers = {}) {\n  return new Response(JSON.stringify(value), { status, headers: { \"content-type\": \"application/json; charset=utf-8\", \"cache-control\": \"no-store\", ...headers } });\n}\n\nfunction secure(response) {\n  const headers = new Headers(response.headers);\n  headers.set(\"x-content-type-options\", \"nosniff\");\n  headers.set(\"referrer-policy\", \"no-referrer\");\n  headers.set(\"x-frame-options\", \"DENY\");\n  headers.set(\"content-security-policy\", \"default-src 'self'; style-src 'self'; script-src 'self'; connect-src 'self'; img-src 'self' data:; frame-ancestors 'none'; base-uri 'none'; form-action 'self'\");\n  return new Respo";
